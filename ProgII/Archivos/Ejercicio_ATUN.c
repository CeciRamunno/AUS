#include<stdio.h>


int main()
{
    // insertar(legislador, conj q corresp)
    // suprimir(legislador, conj q corresp)
    // bool miembro(legislador, conj)
    // mostrar() lee arch y muestra pantalla
}
